export const trackStyle = ({ theme }) => ({
  top: 0,
  bottom: 0,
  right: 0,
  backgroundColor: theme.colors.uiGradeout,
});

export const thumbStyle = ({ theme }) => ({
  backgroundColor: theme.colors.uiPrimary,
});
